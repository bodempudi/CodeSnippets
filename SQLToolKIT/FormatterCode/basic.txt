private static bool IsCommentToken(
    TSqlParserToken token)
{
    if (token == null)
    {
        return false;
    }

    return
        token.TokenType == TSqlTokenType.SingleLineComment ||
        token.TokenType == TSqlTokenType.MultilineComment;
}
